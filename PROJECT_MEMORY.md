# 项目长期记忆

## 用户问题与产品定义
用户阅读 ChatGPT、Claude、Gemini 回答时，会遇到夹杂英文。产品是小型 reading utility / dictionary：划词立即看到中文与音标，点击朗读；短语和段落使用端侧翻译。目标用户是频繁阅读英文、希望低摩擦理解含义的中文读者。

优先级：延迟 > 交互摩擦 > 可靠性 > 常用词义准确 > 视觉简洁 > 高级功能。

## 可复用工作
仓库不以商业增长或用户锁定为主要目标。它是一份可以工作的现成解法，供未来用户和 AI coding agents（AI 编程代理）在理解现有代码后做小范围修改，避免重新实现同等功能。文档应降低接手成本，具体实现优先于提前搭建抽象框架；不规划获客漏斗或 SaaS 商业路线。

## 核心场景
双击单词或划选文本 → 弹窗立即出现 → 本地词条同步填入，或句子译文异步填入 → 用户主动点击朗读原英文。连续划词应自然稳定。

## 已确定架构
- Chrome Manifest V3，桌面 Chrome 138+，原生 TypeScript，不用 UI 框架。TypeScript/esbuild/Playwright 是锁定的开发依赖。
- 顶层页面 isolated content script；只以 Selection/Range 与标准事件捕获，不依赖聊天网站 CSS 类。
- 单词：构建时裁剪 ECDICT，以紧凑 JSON 随脚本预加载；同步属性查询；零 HTTP、零后台消息。
- 精确词条优先，随后 ECDICT exchange:0 原形映射，再尝试保守词尾规则；不声称完整 NLP。
- 短语（包括 gross margin）、句子、段落：独立 TranslationService，Chrome Translator API，en → zh（简体）。功能检测、下载/激活提示、可重试失败；无远端 fallback。
- SpeechSynthesis 点击时调用，先 cancel，只使用浏览器明确报告 localService=true 的英文语音。没有则提示不可用，不使用隐式默认或非本地 fallback；不等待语音初始化、不自动播放。
- 单一 Shadow DOM 弹窗，manual popover 顶层展示，纯 CSS 跟随系统明暗模式。
- 每次选择/关闭推进版本并 abort；只有当前版本可以更新 UI。已创建的翻译会话复用，结果内存缓存最多 32 条，不持久保存文本。

## UX 不变量
- 弹窗不等待翻译；普通词条同步呈现；定位不得超出可视区域。
- 新选择复用节点；鼠标拖选期间保留旧卡片直至新选择完成，避免先关再开的闪烁。
- 内部点击保留弹窗与原选区；外部完成点击、Escape、页面滚动、视口变化关闭。
- 输入框和可编辑区域不触发；空选择/非英文选择不弹出。
- 所有用户和词典文本通过 textContent；不把选中文字插入 HTML。
- 模型准备需要激活时提示点击；诚实报告可用性，不伪造翻译。downloadable 不等于设备磁盘上没有模型，Chrome 可能隐藏语言包状态。
- 原始音标不包裹人为斜杠、不替换记号；项目源码 MIT 与上游词典许可/溯源说明分开保存。

## 非目标
不做整页翻译、侧栏、PDF、OCR、词本、复习系统、LLM 解释、聊天、登录、云同步、服务器、API key、订阅或遥测平台。

## 边界与尚未确定的长期问题
- ECDICT 是通用字典，缺少上下文消歧；现有词义和音标质量不统一。音标保留来源表示，不编造美式读音。
- 词典为常用词子集，不保证罕见术语覆盖；是否增加更大按需本地词库，需要先测内存与冷启动。
- Translator 可用性取决于浏览器、设备、页面策略和模型状态；首个版本不绕过站点策略。
- 明暗主题跟随操作系统，暂不读取各聊天网站自身的主题设置。
- 词典上游内容来源并非本项目完全可控，未来商业使用前重新审视溯源；不把许可声明当作法律风险已解决的结论。

这份文件只保存产品和架构稳定事实。测试结果、动态进展与当前待办见 TASK.md。
